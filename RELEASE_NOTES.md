# 办公工具箱 v1.0.2

四个模块的界面精简与统一 —— 切换模块时不再出现「两层顶栏」。

## 本次更新

**界面精简（四个模块）**
- 去掉每个模块自带的顶栏 / 品牌头 / 头部横幅。原先每个模块都会画一条自己的标题栏，
  叠在外壳顶栏下面，切换时很跳；现在整个工具箱只有顶部那一条。
- 去掉模块内的重复版本号文字（版本由外壳顶栏统一显示）。
- 模块内不再有「检查更新」按钮 / 菜单 / 文字链接 —— 更新统一由工具箱顶栏负责，
  四个模块一起升级。

**风格统一**
- 四个模块的强调色统一为 `#2563EB` 亮蓝（原先各用一套蓝）；圆角半径、按钮高度、
  卡片留白、边框与轨道色一并拉齐。
- 「图片转 Excel」的按钮、进度条、复选框、日志区改用与其它三个模块相同的
  自绘圆角控件，不再是直角按钮 + 系统进度条。结果表格仍用原生表格控件。

**功能没有任何变化** —— 四个模块的下载 / 识别 / 汇总 / 转换逻辑与上一版完全一致。

## 包含的工具

| 模块 | 版本 |
|------|------|
| 法院文书下载器 | v2.5 |
| 发票二维码识别下载工具 | v5.2.0 |
| 发票识别汇总工具 | v1.3.3 |
| 图片表格转 Excel 助手 | v1.2.0 |

## 下载

根据使用习惯选择一种：

| 版本 | 适用场景 | 文件 |
|------|---------|------|
| **单文件版** | 便携 U 盘 / 不常开 | `OfficeToolbox-1.0.2-win-single.exe` (142 MB) |
| **目录版** | 固定机常用，启动快 | `OfficeToolbox-1.0.2-win.zip` (162 MB，解压后 319 MB) |
| **安装版** | 装机用，会建快捷方式和注册表卸载项 | `OfficeToolbox-1.0.2-setup.exe` (172 MB) |

首次运行如遇 SmartScreen 提示，右键 exe → 属性 → 勾选「解除锁定」即可。

## 主要功能

- 左侧图标栏点击切换四个模块，可拖动调整顺序
- 各模块功能与原独立工具完全一致
- 顶栏一键「检查更新」
- 模块在后台跑任务时图标显示绿点，切换不中断
- 关窗前若模块还在运行会弹确认
